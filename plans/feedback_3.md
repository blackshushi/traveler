1. in expense, allow user to choose currency as well.
2. for files in event, allow to pin for member. for example ticket, it can be point to member, so when open the list, the member tag should be shown and user can easily select.
3. sort trip with the trip time instead of the order of creation. sort it by time ascending.
4. Remove the ... in right side of every trip. Make the content direct editable instead. and replace with a delete button, ask before really deleting.
5. Design: 
    - Journal: Make the Date and time at left side, detail at right side, show user tag to mention who is splitting the bill. make use of the space. don't just keep stacking it. you can put happy, this kind of things beside the title. expenses the second line.
    - Plan Add a central line to seperate every day
    - Traveler: make it centralize 
    - First page: delete the Trips. make the + button same level with Traveler. No need > symbol. Make the content round.