1. The tag for + Add tag and user tag design is not align. fix it. use + is enough, Add tag is no needed.
2. User tag is blocking the photos in event files window. fix it.
3. In expenses new page, there's duplicated tag, 5 ways and All. make it just 1 All if all members is included. fix it.
4. In Files tab, implement the same with the event add files/photos. seperate tab and show photos with preview instead of file name. Group photos with event as well. sort by event time as well.